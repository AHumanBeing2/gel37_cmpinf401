import javax.swing.JOptionPane;

public class lab4 {

	public static void main(String[] args) {
		int power;
		
		String L = JOptionPane.showInputDialog("Input the value you want to calculate for log");
		String B = JOptionPane.showInputDialog("Input Base");
		double l = Double.parseDouble(L);
		double b = Double.parseDouble(B);
		
		while(x>0 && b >1)
			for(double i=0; Math.pow(b, i)<= l;i++)
			
			
		System.out.println(power);
	}

}
